처음 컴퓨터 키면,
1. git pull
2. git branch -> 현재 rl 브랜치로 되어있는지 확인
2.1 git switch rl  -> rl브랜치가 아니면 rl 브랜치로 이동

내용 수정 및 작성 후
3. git add .
git commit -m "내용작성"
git push origin HEAD


main 브랜치 코드를 나의 로컬 PC에 적용하여 로컬 PC의 코드를 업데이트하는 방법
1. 로컬 main 브랜치를 최신 상태로 업데이트
git switch main
git pull origin main

2. 자신의 작업 브랜치(rl)로 돌아와 main을 rebase
# rl 브랜치로 다시 이동
git switch rl
# rl 브랜치의 기반을 최신 main으로 재설정함
git rebase main

test
